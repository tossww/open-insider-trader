# Audio Notifications

Audio alerts when Claude needs attention or completes tasks.

**Setup:** Copy `.env.sample` to `.env`, uncomment your TTS service, add API key. See `.env.sample` for all options.
