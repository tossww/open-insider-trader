---
description: Commit changes and push to remote repository
---

Launch git-commit-agent to analyze changes, generate commit message and log entry, commit locally, then push to remote.

Use the Task tool to invoke the git-commit-agent subagent. Pass parameter indicating push is requested.
