---
description: Commit changes locally (no push to remote)
---

Launch git-commit-agent to analyze changes, generate commit message and log entry, then commit locally (no push to remote).

Use the Task tool to invoke the git-commit-agent subagent.
